import { CSSProperties } from 'react';

export const addTaskWrapperStyle: CSSProperties = {
  marginTop: 4,
  minWidth: 0,
  maxWidth: '100%',
};

export const addTaskCardStyle: CSSProperties = {
  minWidth: 0,
  maxWidth: '100%',
  width: '100%',
  boxSizing: 'border-box',
};

export const addTaskInputWrapperStyle: CSSProperties = {
  minWidth: 0,
  width: '100%',
};

export const addTaskInputStyle: CSSProperties = {
  width: '100%',
  boxSizing: 'border-box',
  minWidth: 0,
};
